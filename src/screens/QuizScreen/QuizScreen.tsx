import React from 'react';
import { View, Text } from 'react-native';
import styles from '../styles';

const QuizScreen = () => (
  <View style={styles.container}>
    <Text>You are in the Quiz Screen</Text>
  </View>
);

export default QuizScreen;
